<?php 
/**
 * Admin partial: import-options placeholder.
 *
 * Currently only guards against direct access and renders nothing — a stub kept
 * for the import-options settings surface / future use.
 *
 * @package    mlsimport
 * @subpackage mlsimport/admin/partials
 */

// Block direct access outside of WordPress.
if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly
}
?>