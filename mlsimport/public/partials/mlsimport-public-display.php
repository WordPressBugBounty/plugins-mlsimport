<?php 
/**
 * Public-facing partial for the MLSImport plugin (WordPress plugin-boilerplate stub).
 *
 * This is the conventional markup partial that the boilerplate reserves for the
 * plugin's public output. It currently contains no markup — the standalone
 * front-end views are rendered elsewhere — and only enforces the direct-access guard.
 */
// Block direct requests: bail unless WordPress is bootstrapped.
if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly
}
?>